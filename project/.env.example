# Copy this file to .env and replace placeholders with local values.
# The real .env is ignored by Git and must never contain committed secrets.
DATA_DIR_RAW=data/raw
DATA_DIR_PROCESSED=data/processed
